/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    images: {
      domains: ['gtuwqwohqrzqpgzencnd.supabase.co'], // Add your Supabase hostname here
    },
};

export default nextConfig;
